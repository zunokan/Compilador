programa {
	inteiro x = 500;
	inteiro vetor[10];
	string zuno = "Compiladores";    
	se (x > 3) {
        x = x * 2;
    } senao se (x > 2) {
        x = x / 1;
    } senao {
		x = x -1;
		escreva("Teste");
	}
	
    enquanto (x < 10 && x != 5) {
        x = x + 1;
        y = x + var;
        enquanto (x < 10 && x != 5) {
            x = x + 1;
            y = x + var;
            baz = (verdadeiro && falso) -> verdadeiro;
        }
    }
}
